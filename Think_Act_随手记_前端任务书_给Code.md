# 随手记(收件箱)· 前端任务书(给 Code)

> 视觉/交互基准:`随手记_设计_v2.html`(四屏:记一笔 / 收件箱 / 早上浮现 / 桌面)。视觉 token 沿用 v9 定稿(米白 #FBF5F0、陶土、pill、卡片 16px、暖阴影),语气是搭子(不命令、无感叹号)。
> 产品红线:**收件箱 ≠ 任务**(不计完成/不超时/不推送);**单日聚焦不破**(收件箱不是周视图,没有日历/时间轴)。
> **先对账**:「五、接口契约」与 Cursor 任务书一致,开工前对齐锁定再并行。
> 平台:**本期只做 Android**(widget/快捷方式 Android 专属;iOS 后续)。

---

## 一、记一笔(捕捉面板)

- 底部半屏 sheet 弹出,背后页面虚化;**目标 3 秒记完收起手机**。
- 语音按住说(复用秒应即时反馈)/ 可打字;转写实时上屏。
- 说完调 `inbox-capture`,返回的日期渲染成 **chip**(如「周四 6/12 · 下午」),旁边「改时间」虚线 chip 可改(date + 上午/下午/晚上,简单选择器即可)。
- 没抽到日期 → chip 显示「没定日子」,可手动补。
- 主按钮唯一:**「放进收件箱」**;次按钮「再说一句」(追加口述,合并为同一条)。
- 提示语(固定文案):`先放进收件箱,到那天早上我再提你 —— 今天的计划不动。`
- **绝不丢笔记**:网络/抽取失败时,原文以"没定日子"本地暂存并提示(真实错误,不伪装成功),网络恢复后补同步。
- 入口(本期全做):
  1. 今天/执行屏**顶栏收件箱图标**旁的捕捉入口(图标见 demo ③ 顶栏;点图标进收件箱,收件箱内 FAB「+」开捕捉);
  2. **桌面长按 App 图标**快捷方式「记一笔」直达本面板(deep link,冷启动也要快);
  3. (widget 点击进收件箱,见第四节)。

## 二、收件箱页

- 分组(客户端按 due_date 算,本地今天为锚):**到日子了**(pending 且 due ≤ 今天,组标带数字,条目暖色微高亮)→ **这周** → **以后** → **没定日子**。
- 条目:文本 + 日期 chip + ✕ 删除;`dismissed` 的条目淡显安静躺着(不再计数、不再召回)。
- 底部说明文案:`「到日子了」的事处理一件少一件;其他的不用管,过好今天就行。`
- 右下 FAB「+」开记一笔。
- 失败态:加载失败显真实错误 + 重试(不伪装空态——历史页那个教训)。

## 三、早上浮现(召回卡)

- morning 加载时取 `pending && due ≤ 今天` 的条目,顶部渲染**召回卡**(样式见 demo ③):`你周二记过` + 事项 + `今天 · 下午`。
- 按钮两个:**「加进今天」**(调 `inbox-update.add_today`,由后端生成真实任务并返回,刷新提案/计划;失败显后端 reason)/ **「先不」**(`dismiss`,卡片安静收起,当天不再出现,永不自动再提)。
- 多条到期 → 卡片纵向排,最多先显 3 条,余下折叠「还有 N 条在收件箱」。
- **不自动加入**:没点「加进今天」绝不动今天的计划(变更必须用户确认的老原则)。

## 四、桌面小组件(Android,重点)

- Glance/AppWidget,样式按 demo ④:暖米白圆角卡,头部 `logo + 收件箱` + 右上**角标**,主体**循环播放**条目(事项一行省略 + 日期一行,过期标「过期了」),底部小圆点指示。
- 播放内容:`pending` 条目,**到期的优先**,其后按 due 近→远,最后没定日子;3–4 秒一条,淡入淡出。空收件箱 → 显示一句安静文案(如 `没什么挂着的事`)。
- 点击:整卡点击 → 打开收件箱页(deep link)。
- 刷新:数据变化时(capture/add/dismiss/delete 后)主动更新 widget;另加周期性兜底刷新(系统允许的最低频率即可,别耗电)。
- 角标数 = 与 App 内同一口径(`inbox-badge`)。

## 五、长按 App 图标快捷方式(Android App Shortcuts)

- 「✎ 记一笔」→ deep link 直达捕捉面板;
- 「▤ 收件箱」→ deep link 收件箱页(动态 shortcut 上带角标数,系统支持的范围内)。

## 六、角标(统一口径)

- **count = pending 且 due_date ≤ 本地今天**;没定日子不计。
- 出现位置:今天/执行屏顶栏图标、widget、长按菜单。处理一件(add/dismiss/delete)立即减一、三处同步。
- count=0 时**不显示**角标(平时安静)。

## 七、接口契约(与 Cursor 一致,改动需双方确认)

1. `POST inbox-capture { raw_text, client_local_date, client_tz }` → `{ id, title, due_date?, due_part?, extract_failed? }`
2. `GET inbox-list` → 非 deleted 全量,客户端分组
3. `POST inbox-update { id, action: add_today|dismiss|delete|set_due, ... }`;`add_today` 失败回明确 reason
4. `GET inbox-badge` → `{ count }`(pending && due ≤ today)

## 八、验收(✅ = 真机端到端 + 证据)

- [ ] 真机:长按图标 →「记一笔」→ 说「周四下午发报告」→ chip 显周四下午 → 放进收件箱,**全程 ≤ 5 秒**
- [ ] 断网捕捉:原文不丢(本地暂存 + 真实提示),恢复后入箱
- [ ] 收件箱分组正确;dismissed 淡显且不计数
- [ ] 到日子那天早上:召回卡出现;「加进今天」→ 任务真进当日计划(DB 可查 `source=inbox`);「先不」→ 当天不再出现、次日也不再提
- [ ] 角标三处(顶栏/widget/长按)同数,处理一件三处同步减一,0 时不显示
- [ ] widget 真机循环播放、点开进收件箱、capture 后数秒内刷新
- [ ] 收件箱条目永不出现在执行屏/复盘/历史计数中(红线)
- 证据:真机录屏(捕捉全程 + widget 循环)+ DB 截图 + build hash

## 九、明确不做(本期)
- iOS widget / 快捷方式;没定日子的 AI 择机提醒(自律引擎);周期/重复事项;收件箱搜索与批量操作。
