# Think & Act ｜通宵工作单(给 Code + Cursor)

> 老板今晚休息,你俩按下面顺序自己往下推,**留好证据**,明早老板按效果验收。

## 工作准则(请严格遵守)
1. **按各自队列顺序推进,不空等**;某项卡住就跳下一项,别停摆整条队。
2. **先对账再并行**:需要对齐契约的(建议项表示法)**先两边对齐、写进契约文档,再各自建**——别又漂移(ASR 那次教训)。
3. **✅ = 真端到端,不是"组件测过"**:每项做完**留证据**——build hash + DB 截图 / 录屏 / 日志 / 输入输出对照。**不接受自我声明"完成"。**
4. **遇到歧义 / 卡点 / 要改方向**:写进 `通宵进度.md`(每项一条:做了什么、卡在哪、留了什么证据),**跳到下一项**,别擅自拍方向。
5. **不可逆动作绝不擅自做**:部署生产、改 schema、删数据、动 key——**记下来留给老板**。
6. 一切文案守搭子语气:不挑刺、不命令、不数用户没做、无感叹号。

---

## 先对账(两边先做这一步)
**建议项「未接受」的表示法**: ✅ **已锁定 `status=suggested`** — [`BE_tasks_软建议_status_suggested_契约.md`](./BE_tasks_软建议_status_suggested_契约.md)  
(plan-revise v1.3 契约已 sign-off,不用再对。)

---

## Cursor 队列(后端)
1. **plan-revise reject_reason**:输出 `applicable`/`reject_reason`,`applicable=false` **一律带 reason**(别静默拒);HTTP 错误保留 `error.message`。→ 解锁明早 actual_start 联调。
2. **day-summary v2 缺陷修复**(见《day-summary v2 缺陷修复》):同源算当前任务集(截图那组 = 3/X,绝不再 2/1)、夸点名真做成的、**advice 只扎根真实今日数据否则不出**、**计数排除建议项**、prompt 卫生。**留几组输入→输出对照**。
3. **建议项分离 · BE**(对账后,见《建议项与任务分离》):plan-generate 把建议标"软/未接受"、count/push 排除未接受建议、接受动作落地。
4. 历史视图按日期读 → ✅ **FE 直连 REST**，见 [`BE_历史视图_数据读取_给Code.md`](./BE_历史视图_数据读取_给Code.md)。

## Code 队列(前端)
1. **历史视图**(见《历史视图规格》):近 7 天 → `做成/共`(**跳过排除、建议排除**)→ 柱状(实色+淡纹)+ 每日卡片 + 展开 + 空状态;入口=顶部图表图标。自测(demo 那组 + 真数据,周六须 3/3)。
2. **★ 系统提醒**(见《系统提醒规格》):iOS/安卓本地定时通知(★ 才推、计划变重排、done/skip 取消)+ 首次白名单引导;建议项永不推。自测(到点能响)。
3. **建议项分离 · FE**(对账后):软建议显示软 + "加入"升级;不进复盘打勾行 / 执行屏当前项 / 推送。自测。
4. (plan-revise FE 已完成)→ actual_start 联调 = **明早**(需真机,见下),今晚别等它。

---

## 留给老板明早(不阻塞过夜)
- **actual_start「做不了」真机联调**(等 Cursor reject_reason + Code 已完成都在):验四样——①「把当前这件挪到 X」真能挪 ②说不匹配的话 → 照实显示 BE reason ③后端拒绝**不再**显示"没听出"(盯 FeDebug.reject)④拿其它真实"做不了"例子用探照灯跑一遍。
- 各功能真机录屏 + 老板按效果拍 done。

---

## 老板早上验收清单
1. **actual_start 联调** 四样过(上面)。
2. **day-summary**:截图那组数字对(3/X)、夸具体、advice 要么扎根真实数据要么不出现、0 完成日不空吹。
3. **建议项**:"休息一下"显示软、可加入;不进计数/打勾/推送;加入后变真任务。
4. **历史视图**:`做成/共` 算对(跳过不算漏、建议不计)、柱子实色+淡纹、无百分比、差日子不评判、空状态暖。
5. **★ 系统提醒**:★ 任务到点响(iOS + 安卓开白名单)、计划改重排、done/skip 取消、建议不响。
6. 看 `通宵进度.md` 里两边留的卡点 / 待老板拍的项。
