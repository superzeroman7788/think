# Think & Act ｜通宵进度（Code + Cursor）

> 工作单：**[`Think_Act_通宵工作单_给Code与Cursor.md`](./Think_Act_通宵工作单_给Code与Cursor.md)**  
> 更新：**2026-06-07 夜** — 按队列留卡，明早老板验收。

---

## 先对账 · 建议项表示法

| 项 | 状态 | 证据 |
|---|---|---|
| `status=suggested` vs `accepted` 布尔 | ✅ **锁定 suggested** | [`BE_tasks_软建议_status_suggested_契约.md`](./BE_tasks_软建议_status_suggested_契约.md) |

---

## 内测 v0.1 Bug 派单（修订版）— 2026-06-08 修

| ID | 修复 |
|---|---|
| N-01 | 执行页失败显错+「再试一次」；无假任务 |
| N-02 | `markTaskDone` 无真实起点时不写 `actual_start` |
| N-03 | 已有 `status=suggested` + 提醒/执行排除 |
| N-04 | 语音重生成保留 `manual_*`；加一项成功/失败 banner |
| N-05 | 复盘：点按 planned↔done；长按跳过 |
| N-06 | 历史加载失败显错+重试（不再伪装空态） |
| N-07 | 通知权限 `notifPerm.request()` 已启用 |
| C-01 | BE 已上（此前） |

**待真机回归**：N-04/N-05/N-06 各一条。

---

## Cursor 队列

| # | 项 | 状态 | 证据 / 备注 |
|---|---|---|---|
| 1 | plan-revise `applicable` / `reject_reason` | ✅ **已部署** | `plan-revise` prod；[`plan_revise_acceptance.sh`](../supabase/tests/plan_revise_acceptance.sh) TEST 7 |
| 2 | day-summary v2 缺陷（同源、夸、advice  grounded、排除建议） | ✅ **已部署** | [`day_summary_v2_test.ts`](../supabase/functions/_shared/review/day_summary_v2_test.ts) 6 passed；[`Think_Act_day-summary_v2_缺陷修复_给Cursor.md`](./Think_Act_day-summary_v2_缺陷修复_给Cursor.md) |
| 3 | 建议项分离 · BE | ✅ **已部署** | migration `0016`；`plan-generate` suggestion `status=suggested` |
| 4 | 历史视图数据读取 | ✅ **FE 直连 REST，无需新 API** | [`BE_历史视图_数据读取_给Code.md`](./BE_历史视图_数据读取_给Code.md)；`historyDayStats` 单测 |
| 5 | **随手记(收件箱) · BE** | ✅ **已部署** | migration `0017`；4 个 Edge Fn；[`BE_随手记_接口契约_给Code.md`](./BE_随手记_接口契约_给Code.md)；[`inbox_acceptance.sh`](../supabase/tests/inbox_acceptance.sh) PASS |

**Cursor 队列**：随手记 BE 已交付，等 Code FE 真机。

---

## Code 队列

| # | 项 | 状态 | 证据 / 备注 |
|---|---|---|---|
| 1 | **历史视图**（7 天、做成/共、柱图、卡片、空状态） | ✅ **真机验过**（build `0de24465`） | FE 直连 REST：`HistoryRepository`/`HistoryViewModel`/`HistoryScreen`；入口=早上屏顶部图表图标。**真机截图**:demo 组(3 done+1 skip+1 suggested)→ **今天 3/3**(非 3/4);累计=9=5+3+1;柱实色+斜纹+今天高亮;卡片暖总结+展开明细(跳过中性灰)+提醒;空天「歇了一天」不评判;无百分比。`historyDayStats` 对齐 BE。证据图 `/tmp/tna_history2.png`(待挪 test-evidence) |
| 2 | **★ 系统提醒**（iOS UN / Android Alarm+白名单引导） | ✅ **真机验过（到点真响）** | `reminders/`:`ReminderScheduler`(接口)+`ReminderPlanner`(仅★+planned+未来)+Android `AlarmManager` exact+`ReminderReceiver`+通知渠道+`NotificationPermission`(POST_NOTIFICATIONS)+iOS `UNUserNotificationCenter`(编译过)。manifest 加权限+receiver。执行屏/完整计划 load/done/skip/重排 → `sync` 整组重排;首次白名单引导一屏。**真机证据**:seed ★+非★ 同点 → dumpsys notification 显 `★ 写周报` 来自渠道 `tna_star_reminders`,**非★ 不弹**。 |
| 3 | 建议项分离 · FE | 🟡 **大部分已合** | 早上屏软样式+「加入」[`MorningScreen.kt`](../shared/src/commonMain/kotlin/com/thinkandact/ui/morning/MorningScreen.kt)；落库 `status=suggested` [`PlanRepository.kt`](../shared/src/commonMain/kotlin/com/thinkandact/data/PlanRepository.kt)；执行/复盘 REST 排除 suggested。**待真机录屏证据** |
| 5 | plan-revise actual_start 联调 | ⏸ **明早** | BE reject_reason 已上；验工作单四样 |
| 6 | **随手记(收件箱) · FE** | ⏳ **BE 已上，待接** | 契约 [`BE_随手记_接口契约_给Code.md`](./BE_随手记_接口契约_给Code.md)；捕捉/收件箱/召回/widget/shortcut |
| 7 | **执行屏 · 环心分钟倒计时 + 三态文字** | ✅ **真机验过**（build md5 `128c9430`） | 纯增量、定稿视觉零改值。环心改三态**时钟制·无状态**倒计时（由 planned_start/end+now 当场算，只跳分钟）。真机三态截图全过：**未开始** 21分/距开始、**进行中** 22分/距结束、**超时** 超·23分·该收尾了?(「超」柔暖半透不刺眼)；改时间 21→**45分**立即对得上(证明无状态非缓存)；duration=null→**环心不出现倒计时**；潮水/星环/彗星/光晕无回退。证据 `docs/test-evidence/环心倒计时_*.png`(5 张) |

---

## 留给老板明早

1. **actual_start 联调** 四样（真机）
2. **day-summary** 截图场景（3/X、无深度活儿瞎编）
3. **建议项** 真机五样
4. **历史视图** — Code 做完后录屏
5. **★ 系统提醒** — iOS + Android 各一台录屏
6. 本文件卡点项

---

## 歧义 / 待老板（无擅自拍板）

| 项 | 说明 |
|---|---|
| 历史「暖总结 one 行」 | 本版 FE 本地生成；若以后要 BE 存字段再开 |
| Android 厂商通道 JPush | 系统提醒规格已标 fast-follow，本版不做 |
| `skipped` 在历史卡片 | 展开显示、中性灰；计数排除 — 已写进规格 |

---

## 不可逆动作记录

（今晚无擅自 db push / deploy 以外的生产变更；此前 Cursor 项均已按工作单部署并留单测证据。）
