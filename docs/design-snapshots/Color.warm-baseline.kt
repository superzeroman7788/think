package com.thinkandact.app.ui.theme

import androidx.compose.ui.graphics.Color

object TnaColors {
    val Background = Color(0xFFFCF8F4)
    val Accent = Color(0xFFD95B45)

    // TODO(designer-confirm): Derived warm ink chosen to feel softer than pure black.
    val Ink = Color(0xFF241A16)

    // TODO(designer-confirm): Derived secondary text for notes, labels, and inactive controls.
    val InkSoft = Color(0xFF6A5A52)

    // TODO(designer-confirm): Derived muted text for timestamps, section labels, and metadata.
    val Muted = Color(0xFF9A8A82)

    // TODO(designer-confirm): Derived pale muted color for placeholders and disabled states.
    val MutedSoft = Color(0xFFCDBEB5)

    // TODO(designer-confirm): Derived card surface that keeps the warm paper feeling.
    val Surface = Color(0xFFFFFFFF)

    // TODO(designer-confirm): Derived warm line color for subtle dividers and component outlines.
    val Line = Color(0xFFE9DDD4)

    // TODO(designer-confirm): Derived softer divider for dense task rows.
    val LineSoft = Color(0xFFF7EEE6)

    // TODO(designer-confirm): Derived accent wash for selected chips, input focus, and AI highlights.
    val AccentSoft = Color(0xFFF7E4DE)

    // TODO(designer-confirm): Derived deeper accent for small text on pale accent surfaces.
    val AccentDeep = Color(0xFFA94431)

    // TODO(designer-confirm): Derived warm shadow color; keep subtle and diffuse.
    val WarmShadow = Color(0x1F211815)

    // TODO(designer-confirm): Derived AI voice color, intentionally lighter than body text.
    val AiVoice = Color(0xFF76655D)
}
