# 执行屏整页改版 · 规格(给 Code)+ 欠账清算

> 视觉基准:`执行屏_整页改版_v9.html`(最终版,取代 v8)。
> **新规矩(老板定):收到本单先回"收到 + 排期",再动手。**

---

## 〇、先回话再动手 · 欠账清算

真机发现以下**已派规格未落地**(截图实证:环心仍是「超 261分 / 该收尾了?」)。开工前**逐条回状态**(已做/未做/卡点):

1. 《执行屏 环心倒计时与三态文字 规格》(v7 终稿:状态字在上、X小时XX分、超时三行「超过/时长/是否要结束」)
2. 第七批 F7-01(diff 原/新两行格式)
3. 第七批 F7-02(全局时间列起止统一)——此前你回报 v0.1.11 已做并截图,但环心没用上同一个 formatDuration,确认覆盖范围
4. 第七批 F7-03(语音手动替代,B 表 5 项全做已拍板)

---

## 一、本单内容(按 v9 demo 逐项)

**1. 标题**:今天 → **「今日日程」**;字号收小(约 15.5sp),**永不换行**(超长省略)。

**2. 顶栏统一圆图标**(复用 Morning 改版的通用顶栏组件,34dp 圆、线条 SVG、不用 emoji):
- 收件箱(托盘图标 + 右上角标,角标口径 = inbox-badge)
- 完整计划(列表图标)
- 复盘(月亮图标)
- 进度胶囊「2 / 5」:**缩小**(字号 ~10.5、内边距收紧),是状态标签不是按钮
- 原先大小不一的胶囊/裸符号全部替换

**3. 环心文字(= 欠账第 1 条,严格按 demo)**:
- 三态:未开始「距开始 + X」/ 进行中「距结束 + X」(≤10 分钟换"快到时间了")/ 超时三行「**超过 / X / 是否要结束**」
- 时长格式全局共用 formatDuration:<60 分「XX分钟」,≥60 分「**X小时XX分**」(整小时只显「X小时」)
- 时钟制无状态、只跳分钟、暖色不发红——均为既有规格,不复述

**4. 环后常驻柔光(新增,Loora 参考的暖版)**:
- 环正后方一团径向暖晕:`radial-gradient(closest-side, rgba(231,170,108,.34), rgba(231,170,108,.14) 55%, transparent 75%)`,直径约为环的 1.5 倍,轻微 blur
- 呼吸:7s 周期,opacity .75⟷1 + scale .96⟷1.03
- **克制**:不抢星环;★ 重要态时原有 halo 照常叠加
- CMP 实现注意:用 Canvas radialGradient 画,别用运行时 blur(性能红线沿用)

**5. 不动的**:潮水、星环、彗星、星尘、完成态、★ 态、跳过/完成按钮、语音条——v9 定稿视觉一律保持。

## 二、验收(真机 + 证据)
- [ ] **重放截图场景**:超时 261 分 → 环心显示「超过 / 4小时21分 / 是否要结束」
- [ ] 三态各一张截图,文字与 demo 一致;倒计时整分钟跳
- [ ] 顶栏:四元素一行排齐、标题不换行、角标可见、进度胶囊为小号
- [ ] 柔光呼吸可见但克制;低端机不掉帧(沿用 Canvas 一次性绘制原则)
- [ ] 欠账 4 条的逐条回复
- 证据:三态截图 + build hash
