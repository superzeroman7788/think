package com.thinkandact.app.ui.theme

import androidx.compose.ui.graphics.Color

object TnaColors {
    val Background = Color(0xFFFBF5F0)
    val Accent = Color(0xFFB5654A)

    // Locked from color_direction_compare.html direction 2.
    val Ink = Color(0xFF2E2924)

    // Locked from color_direction_compare.html direction 2.
    val InkSoft = Color(0xFF7A7065)

    // Locked from color_direction_compare.html direction 2.
    val Muted = Color(0xFFA89C8E)

    // Locked from color_direction_compare.html direction 2.
    val MutedSoft = Color(0xFFA89C8E)

    // Locked from color_direction_compare.html direction 2.
    val Surface = Color(0xFFFFFFFF)

    // Locked from color_direction_compare.html direction 2.
    val Line = Color(0xFFEADFD4)

    // Locked from color_direction_compare.html direction 2.
    val LineSoft = Color(0xFFF4EBE3)

    // Locked from color_direction_compare.html direction 2.
    val AccentSoft = Color(0xFFF0E2DA)

    // Locked from color_direction_compare.html direction 2.
    val AccentDeep = Color(0xFF8F4F3B)

    // Compose translation of the warm HTML card shadow.
    val WarmShadow = Color(0x295A3C1E)

    // Locked from color_direction_compare.html direction 2.
    val AiVoice = Color(0xFF5E5247)
}
