<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- ★ 任务系统提醒(本地定时通知) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- 时刻点 §四：★ 任务同步系统日历(可选,默认关) -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <!-- 第四批 N-01:重启后重排 ★ 提醒(闹钟被系统清空) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".ThinkAndActApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThinkAndAct">
        <!-- N-03:singleTask——点提醒通知拉起已存在实例时走 onNewIntent,extra 不丢 -->
        <!-- F-15:v0.1 无横屏场景,锁竖屏,顺带终结旋转跳页/旧输入还魂(BUG-11) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ThinkAndAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- 随手记长按图标快捷方式：记一笔 / 收件箱 -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- ★ 任务提醒到点接收器(弹本地通知) -->
        <receiver
            android:name="com.thinkandact.reminders.ReminderReceiver"
            android:exported="false" />

        <!-- 第四批 N-01:开机后重排当天剩余 ★ 提醒 -->
        <receiver
            android:name="com.thinkandact.reminders.ReminderBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 随手记桌面小组件 -->
        <receiver
            android:name="com.thinkandact.app.widget.InboxWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/inbox_widget_info" />
        </receiver>
    </application>

</manifest>
